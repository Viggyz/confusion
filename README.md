# Confusion Restaurant Webapp
A Restaurant webapp that allows users to browse restaurant offerings and like and comment on their favorite dishes.

Can be run with:
    cd Client && npm install
    cd ../Server && npm install
    cd ..
    docker-compose up
